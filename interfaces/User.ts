export interface User {
    id: string;
    role: string;
    username: string;
    password: string;
    buyingHistory: string[];
};

export interface UserToReturn {
    id: string;
    role: string;
    username: string;
    buyingHistory: string[];
};

