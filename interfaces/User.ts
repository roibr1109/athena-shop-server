export interface User {
    id: number
    role: string
    username: string
    password: string
    buyingHistory: string[]
};

export interface UserToReturn {
    id: number
    role: string
    username: string
    buyingHistory: string[]
};

